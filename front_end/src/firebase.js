// Firebase supprimé — les uploads utilisent l'endpoint REST /upload de l'API GraphQL.
// Ce fichier est conservé uniquement pour éviter les erreurs d'import éventuels.

export const storage = null;

export const API_URL = import.meta.env.VITE_API_URL ?? 'http://localhost:4000';
